# gfw_fishing_osw_ais

Python via uv (`uv run`, `uv add` — never bare pip or python).
R via renv. Python code in python/, R in r/.

Never modify anything in data/
Ask before installing new dependencies.
Propose a plan before multi-file changes.